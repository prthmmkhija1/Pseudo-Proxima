# API Reference

This section is generated with `mkdocstrings`.

## Generate locally
```bash
pip install -e .[docs]
mkdocs serve
```

## Modules to expose
- `proxima.core`
- `proxima.backends`
- `proxima.data`
- `proxima.cli`
- `proxima.tui`

Add module pages by including `::: proxima.module` blocks in this folder as needed.
